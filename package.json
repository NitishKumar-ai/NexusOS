{
  "name": "agent-mission-control",
  "version": "1.0.0",
  "description": "Monorepo for Agent Mission Control (OpenClaw + ECC)",
  "private": true,
  "workspaces": [
    "dashboard",
    "packages/*"
  ],
  "scripts": {
    "dev": "docker-compose up",
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "prettier": "^3.0.0"
  }
}
